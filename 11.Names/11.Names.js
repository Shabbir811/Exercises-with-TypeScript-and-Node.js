"use strict";
// Store the names of a few of your friends in a array called names.
let names = ["shoaib", "junaid", "saad", "nadeem"];
// Print each person’s name by accessing each element in the list, one at a time.
names.map(el => {
    console.log(el);
});
